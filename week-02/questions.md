What is a reducer in LangGraph? Why is add_messages one?
Why does the current chat "remember" between turns even though the graph is technically stateless per invocation?
If I remove Annotated[list, add_messages] and just use list[BaseMessage], what breaks?